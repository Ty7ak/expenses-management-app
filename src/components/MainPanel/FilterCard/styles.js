import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  addButton: {
    marginTop: '20px',
    borderRadius: "5em",
    backgroundColor: "#424242"
  },
  clearButton: {
    marginTop: '20px',
    borderRadius: "5em",
  },
}));